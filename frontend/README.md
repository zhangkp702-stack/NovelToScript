# Frontend

This is a formal frontend project built with Vue + Vite.

## Setup
```bash
cd frontend
npm install
```

## Run
```bash
npm run dev
```

Dev server starts at `http://localhost:5173`, and `/api` is proxied to backend `http://localhost:8080`.

## Build
```bash
npm run build
```
