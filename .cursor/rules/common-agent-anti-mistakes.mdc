---
description: 通用防错规则（针对本项目已出现问题）
alwaysApply: true
---

# 通用防错规则（避免重复犯错）

本规则用于约束后续所有代码生成与重构行为，重点覆盖本次开发中已出现的典型错误。  
目标：避免“能编译但不可维护”与“目录、分层、依赖不一致”的问题再次出现。

## 一、包名与目录必须严格一致

1. 每次新增或修改 Java 文件时，`package` 必须与物理目录完全一致。
2. 禁止在同一轮修改中混用多个包前缀（如 `com.zkp.test` 与 `com.zkp.my12306.ntc`）。
3. 任何批量重构后，必须执行全量扫描检查：
   - 是否存在包名不一致；
   - 是否存在旧包残留导入。
4. 若出现不一致，必须同批次全部修复，不允许“修一处留多处”。

## 二、控制器与服务分层边界

1. Controller 层只负责路由、参数接收、响应组装。
2. Service 层不得依赖 Servlet API（如 `HttpServletRequest`、`HttpServletResponse`）。
3. Service 方法签名优先使用 DTO、String、业务对象。
4. 禁止把临时调试接口当正式接口长期保留（如 probe 类接口）。

## 三、MyBatis-Plus 使用约束

1. 使用 MyBatis-Plus 时，Mapper 必须是 `interface` 且继承 `BaseMapper<T>`。
2. 项目必须具备可用的 `SqlSessionFactory`（自动配置或显式配置至少一种）。
3. 启动类必须配置 `@MapperScan`，扫描路径与 Mapper 包一致。
4. 新增 MyBatis 配置后必须做启动级验证，而不仅仅是静态代码检查。

## 四、单控制器聚合约束（认证模块）

1. 同一业务域优先单控制器聚合（如 `AuthController` 统一管理登录/登出/当前用户）。
2. 禁止在无业务必要时拆分多个语义重复的控制器（例如 `AuthSessionController` 与 `AuthProbeController`）。
3. 若确有拆分需求，必须在文档中说明拆分依据与职责边界。

## 五、修改后强制验证清单

每次涉及包重构、分层调整、ORM 替换时，必须按顺序执行：

1. 包名一致性扫描（全项目）。
2. 编译验证：`mvnw -DskipTests compile`。
3. 测试验证：`mvnw test`（若存在测试）。
4. 启动验证：至少一次应用启动，确认无 Bean 装配错误。
5. 将本次问题与修复结论记录在 PR 描述中。

## 六、执行原则

1. 不允许为了“先跑通”引入明显违反分层和规范的临时代码而不回收。
2. 若用户明确指出结构要求（如 service 接口 + impl），必须优先满足结构要求，再补功能细节。
3. 若发现此前改动与当前规则冲突，必须立即修正，不得带病继续堆功能。
